{% extends 'base.html.twig' %}

{% block title %}Tous les Articles!{% endblock %}

{% block content %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
</style>

<div class="example-wrapper">
    <div>
        <form class="d-flex justify-content-center m-5">
            <a class="btn btn-dark mr-2" href="{{ path('showOneArticle', {'idArticle': '1'}) }}" role="button">One Article</a>
            <a class="btn btn-dark mr-2" href="{{ path('updateArticle', {'idArticle': '1'}) }}" role="button">Update Article</a>
            <a class="btn btn-dark mr-2" href="{{ path('deleteArticle', {'idArticle': '1'}) }}" role="button">Delete Article</a>
        </form>
    </div>
</div>
{% endblock %}
