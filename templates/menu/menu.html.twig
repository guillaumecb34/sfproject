{% block menu %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                <li class="nav-item active">
                    <a class="btn btn-outline-dark" href="{{ path('blogArticle') }}" role="button">Article</a>
                    <a class="btn btn-outline-dark my-2 my-sm-0" href="{{ path('blogAuteur') }}"
                       role="button">Auteur</a>
                </li>
            </ul>
            {% if app.user %}
                <h5 class="mr-2" >Bonjour : {{ app.user.username }}</h5>
                <a class="btn btn-outline-dark my-2 my-sm-0 ml-2" href="{{ path('app_logout') }}" role="button">Déconnexion</a>
            {% else %}
                <a class="btn btn-outline-dark my-2 my-sm-0 mr-2" href="{{ path('app_login') }}" role="button">Connexion</a>
                <a class="btn btn-outline-dark my-2 my-sm-0" href="{{ path('app_register') }}" role="button">Inscription</a>
            {% endif %}
        </div>
    </nav>
{% endblock %}